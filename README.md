## Pages
- `/` — the essay, from `src/content/`
- `/quiz` — "Ways to Pace", the branching-point walk. All copy lives in
  `src/quiz/content.js`; the optional Supabase tally is set up in
  `src/quiz/config.js` (see `src/quiz/SUPABASE.md`).
