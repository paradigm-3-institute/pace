---
import type { CollectionEntry, RenderResult } from "astro:content";

interface Props {
  id: string;
  title: string;
  textClass: string;
  content: RenderResult;
}

const { id, title, textClass, content } = Astro.props;
const { Content } = content;
---

<section id={id} class:list={["mb-12 -scroll-mt-8 last:mb-0"]}>
  <h2 style={`background: color-mix(in oklab, var(--color-${id}-bg), white 20%);`} class={textClass}>{title}</h2>
  <div class="mx-auto padded">
    <Content />
  </div>
</section>
