---
interface Props {
  href: string;
  bgClass: string;
  textClass: string;
  label: string;
}

const { href, bgClass, textClass, label } = Astro.props;
---

<li class:list={[textClass, bgClass]}>
  <a class="px-7 py-5 block" href={href}>{label}</a>
</li>
